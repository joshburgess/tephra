//! Phase 5 validation: two-pass rendering with manual barriers.
//!
//! Renders to an offscreen target first, then samples it when rendering
//! to the swapchain.

fn main() {
    env_logger::init();
    todo!("Phase 5 validation example: multi-pass rendering with WSI")
}
