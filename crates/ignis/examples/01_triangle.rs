//! Phase 1+2 validation: spinning rainbow triangle.
//!
//! Creates a window, initializes the Vulkan device, and renders a triangle
//! with transient vertex data and a hardcoded pipeline.

fn main() {
    env_logger::init();
    todo!("Phase 2 validation example: triangle with transient vertex data")
}
