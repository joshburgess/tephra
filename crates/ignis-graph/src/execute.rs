//! Runtime execution of a compiled render graph.

/// Executes a compiled graph against a device, allocating physical resources,
/// emitting barriers, and invoking render callbacks.
pub struct GraphExecutor {
    // TODO: Phase 6, Iteration 6.6
    _private: (),
}
